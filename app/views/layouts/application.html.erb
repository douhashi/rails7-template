<!DOCTYPE html>
<html>
  <head>
    <title>App</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= render partial: 'layouts/shared/head' %>
  </head>

  <body class="">
    <div class="w-full">
      <%= yield %>
    </div>
  </body>
</html>
