<section class="w-full flex items-center justify-center">
  <div class="flex flex-col w-full max-w-md px-4 py-8 sm:px-6 md:px-8 lg:px-10">
    <div class="self-center mb-6">
      <%= component 'ui/headings/h1', title: 'Login to your account', size: 'text-xl' %>
    </div>

    <div class="flex gap-4 item-center">
      <%= component 'ui/button', label: 'Sign in with Google', icon: 'fab fa-google', wide: true %>
    </div>

    <div class="mt-8">
      <%= form_with url: '#' do |f| %>
        <div class="flex flex-col mb-2">
          <%= component 'ui/form/field_with_icon',
            form: f,
            method: :field,
            icon: 'fas fa-envelope',
            field_type: :email,
            html_options: { required: true, placeholder: 'Email' } %>
        </div>

        <div class="flex flex-col mb-6">
          <%= component 'ui/form/field_with_icon',
            form: f,
            method: :field,
            icon: 'fas fa-lock',
            field_type: :password,
            html_options: { required: true, placeholder: 'Password' } %>
        </div>

        <div class="flex items-center mb-6 -mt-4">
          <div class="flex ml-auto">
            <%= link_to '#' do %>
              <%= component 'ui/paragraph', size: 'xs', weight: 'light' do %>
                Forgot Your Password?
              <% end %>
            <% end %>
          </div>
        </div>

        <div class="flex w-full">
          <%= component 'ui/button', label: 'Login', wide: true, button_type: :submit %>
        </div>

      <% end %>
    </div>

    <div class="flex items-center justify-center mt-6">
      <%= link_to '#' do %>
        <%= component 'ui/paragraph', size: 'xs', weight: 'light' do %>
          You don&#x27;t have an account?
        <% end %>
      <% end %>
    </div>
  </div>

</section>
